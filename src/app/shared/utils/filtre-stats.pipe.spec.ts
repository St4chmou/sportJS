import { FiltreStatsPipe } from './filtre-stats.pipe';

describe('FiltreStatsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreStatsPipe();
    expect(pipe).toBeTruthy();
  });
});
