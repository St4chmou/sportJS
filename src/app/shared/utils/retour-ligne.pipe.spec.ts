import { RetourLignePipe } from './retour-ligne.pipe';

describe('RetourLignePipe', () => {
  it('create an instance', () => {
    const pipe = new RetourLignePipe();
    expect(pipe).toBeTruthy();
  });
});
